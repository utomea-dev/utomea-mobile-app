import React from "react";
import { Text, View } from "react-native";
import { ScrollView } from "react-native-gesture-handler";

function Search() {
  return <Text style={{ color: "white" }}>Search Screen</Text>;
}

export default Search;
